services:
  influxdb:
    image: influxdb:1.8
    restart: unless-stopped
    profiles:
      - influxdb
    env_file:
      - .env.influxdb
    volumes:
      - ./influxdb-data:/var/lib/influxdb
    ports:
      - "${INFLUXDB_PORT:-8086}:8086"
    healthcheck:
      test: ["CMD", "influx", "-username", "root", "-password", "root", "-execute", "SHOW DATABASES"]
      interval: 10s
      timeout: 5s
      retries: 10

  grafana:
    image: grafana/grafana:11.5.2
    restart: unless-stopped
    profiles:
      - grafana
    depends_on:
      influxdb:
        condition: service_healthy
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
      GF_USERS_DEFAULT_THEME: light
    volumes:
      - ./grafana-data:/var/lib/grafana
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "${GRAFANA_PORT:-3000}:3000"

  nightscout-mongo:
    image: mongo:4.4
    restart: unless-stopped
    volumes:
      - ./nightscout-mongo-data:/data/db
    healthcheck:
      test: ["CMD", "mongo", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 10

  nightscout:
    image: nightscout/cgm-remote-monitor:latest
    restart: unless-stopped
    depends_on:
      nightscout-mongo:
        condition: service_healthy
    env_file:
      - .env.nightscout
    environment:
      NODE_ENV: production
      PORT: 1337
      HOSTNAME: 0.0.0.0
      MONGODB_URI: mongodb://nightscout-mongo:27017/nightscout
      MONGO_CONNECTION: mongodb://nightscout-mongo:27017/nightscout
      INSECURE_USE_HTTP: "true"
      DISPLAY_UNITS: mmol
      AUTH_DEFAULT_ROLES: ${AUTH_DEFAULT_ROLES:-readable}
      ENABLE: ${ENABLE:-careportal rawbg iob cors}
      CUSTOM_TITLE: ${CUSTOM_TITLE:-Glucose CGM}
      THEME: ${THEME:-colors}
      TIME_FORMAT: ${TIME_FORMAT:-24}

  nightscout-ui:
    image: nginx:1.27-alpine
    restart: unless-stopped
    depends_on:
      nightscout:
        condition: service_started
    volumes:
      - ./nightscout-overlay/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nightscout-overlay/rate-overlay.js:/etc/nginx/overlay/rate-overlay.js:ro
      - ./nightscout-overlay/sw.js:/etc/nginx/overlay/sw.js:ro
    ports:
      - "${NIGHTSCOUT_PORT:-1337}:1337"

  libreview-sync:
    image: node:20-alpine
    restart: unless-stopped
    profiles:
      - libre
    depends_on:
      nightscout:
        condition: service_started
    env_file:
      - .env.nightscout
      - .env.libreview
    environment:
      NIGHTSCOUT_URL: http://nightscout:1337
      MONGODB_URI: mongodb://nightscout-mongo:27017/nightscout
      LIBREVIEW_SYNC_PORT: 8787
      # Poll-interval: 60s i.p.v. 30s. LLU levert maar ~1 waarde/min, dus 2x/min
      # pollen was puur verspilling (54% lege cycli) + trigger voor 430 rate-limits.
      # Staat hier (niet in .env.libreview) zodat het via git deployt; environment
      # overschrijft env_file, dus dit wint van een eventuele oude waarde daar.
      LIBREVIEW_INTERVAL_SECONDS: 60
      # DST-bewuste timestamps via IANA-zone. Lege offset overschrijft een eventuele
      # vaste LIBREVIEW_TZ_OFFSET in .env.libreview, zodat zomer/winter vanzelf klopt.
      LIBREVIEW_TZ: Europe/Amsterdam
      LIBREVIEW_TZ_OFFSET: ""
    working_dir: /app
    volumes:
      - ./:/app
    command: ["node", "scripts/libreview-nightscout-sync.mjs", "--server", "--loop"]
    ports:
      - "8787:8787"
